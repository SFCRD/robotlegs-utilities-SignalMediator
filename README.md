SignalMediator
==============

`SignalMediator` is a simple RobotLegs Mediator subclass that comes with a `SignalMap`.
SignalMap works exactly like an EventMap, except you map signals in it. Then, just
like an EventMap, when the Mediator is removed, all signals are unmapped automatically.